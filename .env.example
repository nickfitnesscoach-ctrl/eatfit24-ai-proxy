# EatFit24 AI Proxy - Environment Variables
# Copy this file to .env and fill in your values
# ALL THREE REQUIRED VARIABLES MUST BE SET - service won't start without them

# === REQUIRED ===

# OpenRouter API key (get from https://openrouter.ai/keys)
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# OpenRouter model ID (must be a valid vision model)
# Examples: openai/gpt-4o-mini, openai/gpt-4o, google/gemini-2.0-flash-001
OPENROUTER_MODEL=openai/gpt-4o-mini

# Authentication key for incoming requests (generate a strong random string)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_PROXY_SECRET=your-secret-key-here-minimum-32-characters

# === OPTIONAL ===

# OpenRouter API base URL (rarely needs to be changed)
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Application name (for logs)
# APP_NAME=EatFit24 AI Proxy

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Maximum image file size in bytes (default: 5MB = 5242880)
# MAX_IMAGE_SIZE_BYTES=5242880
