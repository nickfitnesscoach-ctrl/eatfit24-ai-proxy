# EatFit24 AI Proxy

Микросервис для распознавания еды по фотографии. Принимает изображение и комментарий пользователя,
вызывает AI-модель через OpenRouter и возвращает структурированные данные о продуктах и КБЖУ.

## Что делает сервис

- **Распознаёт еду на фото** — определяет продукты и блюда
- **Считает КБЖУ** — калории, белки, жиры, углеводы
- **Учитывает комментарии** — если пользователь указал веса, AI использует их
- **Поддерживает два языка** — русский (по умолчанию) и английский

## Архитектурная позиция

> AI Proxy — это **stateless внутренний микросервис**.  
> Он **не реализует** пользовательские лимиты и биллинг.  
> Все бизнес-квоты и правила ценообразования — **на стороне backend**.

## Быстрый старт (локально)

```bash
# 1. Клонировать и перейти в папку
cd eatfit24-ai-proxy

# 2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Создать .env файл
cp .env.example .env
# Отредактируйте .env — все три переменные ОБЯЗАТЕЛЬНЫ:
# - OPENROUTER_API_KEY
# - OPENROUTER_MODEL
# - API_PROXY_SECRET

# 5. Запустить сервер
uvicorn app.main:app --reload --port 8001
```

## Переменные окружения

> ⚠️ **Все обязательные переменные должны быть заданы, иначе сервис не запустится.**

| Переменная | Обязательна | Описание |
|------------|-------------|----------|
| `OPENROUTER_API_KEY` | ✅ | API ключ OpenRouter |
| `OPENROUTER_MODEL` | ✅ | Модель (напр. `openai/gpt-4o-mini`) |
| `API_PROXY_SECRET` | ✅ | Ключ для аутентификации запросов |
| `LOG_LEVEL` | ❌ | Уровень логов (default: INFO) |
| `MAX_IMAGE_SIZE_BYTES` | ❌ | Макс. размер файла (default: 5MB) |

📄 Полная документация: [docs/ENV_CONTRACT.md](docs/ENV_CONTRACT.md)

## Примеры запросов

### Health Check
```bash
curl http://localhost:8001/health
# {"status":"ok"}
```

### Распознавание еды
```bash
curl -X POST http://localhost:8001/api/v1/ai/recognize-food \
  -H "X-API-Key: ваш_ключ" \
  -F "image=@food.jpg" \
  -F "user_comment=Индейка 150 г, картофель 200 г" \
  -F "locale=ru"
```

### Пример ответа
```json
{
  "items": [
    {
      "name": "Индейка",
      "grams": 150.0,
      "kcal": 180.0,
      "protein": 30.0,
      "fat": 6.0,
      "carbs": 0.0
    },
    {
      "name": "Картофель отварной",
      "grams": 200.0,
      "kcal": 160.0,
      "protein": 4.0,
      "fat": 0.2,
      "carbs": 36.0
    }
  ],
  "total": {
    "kcal": 340.0,
    "protein": 34.0,
    "fat": 6.2,
    "carbs": 36.0
  },
  "model_notes": "Веса взяты из комментария пользователя"
}
```

## Docker

### Локальный запуск
```bash
# Создать .env
cp .env.example .env
# Отредактировать .env (добавить ключи)

# Запустить
docker compose up -d --build

# Проверить статус
docker compose ps
docker logs eatfit24-ai-proxy

# Остановить
docker compose down
```

### На сервере
```bash
# Deploy происходит автоматически через GitHub Actions при push в master
# Или вручную:
git pull && docker compose up -d --build
```

## Документация

| Документ | Описание |
|----------|----------|
| [docs/API_CONTRACT.md](docs/API_CONTRACT.md) | Контракт API (SSOT) |
| [docs/API_DOCS.md](docs/API_DOCS.md) | Подробная документация API |
| [docs/ENV_CONTRACT.md](docs/ENV_CONTRACT.md) | Переменные окружения |
| [docs/RUNBOOK.md](docs/RUNBOOK.md) | Операционный runbook |
| [docs/CHANGELOG.md](docs/CHANGELOG.md) | История изменений |

## Troubleshooting

### Сервис не запускается
```
pydantic ValidationError: Field required
```
**Причина:** Не заданы обязательные переменные окружения.  
**Решение:** Проверьте `.env` — должны быть `OPENROUTER_API_KEY`, `OPENROUTER_MODEL`, `API_PROXY_SECRET`.

### 401 Unauthorized
**Причина:** Неверный или отсутствующий API ключ.  
**Решение:** Проверьте заголовок `X-API-Key`, он должен совпадать с `API_PROXY_SECRET`.

### 429 Too Many Requests
**Причина:** Превышен лимит запросов к OpenRouter.  
**Что происходит:** Сервис автоматически делает 3 попытки с паузами.  
**Решение:** Подождите 1-5 минут или проверьте лимиты в OpenRouter.

### 500 AI Service Error с timeout
**Причина:** OpenRouter не ответил за 30 секунд.  
**Решение:** Повторите запрос. Если проблема повторяется — проверьте статус OpenRouter.

### 413 File Too Large
**Причина:** Изображение больше 5 МБ.  
**Решение:** Уменьшите размер изображения перед отправкой.

## Безопасность

- ✅ Доступ только через Tailscale VPN
- ✅ API key аутентификация (timing-safe сравнение)
- ✅ Контейнер работает от non-root пользователя
- ✅ Resource limits в Docker
- ✅ Никаких секретов в git (`.env` в `.gitignore`)

## Разработка

```bash
# Запуск с hot-reload
uvicorn app.main:app --reload --port 8001

# Логи в JSON формате с request_id
# Каждый ответ содержит заголовок X-Request-ID
```
