# Отчёт по устранению проблем — EatFit24 AI Proxy

**Дата:** 2025-12-22
**Сервер:** 100.84.210.65 (Timeweb NL / Tailscale)
**Исполнитель:** Claude Code + Nicolas

---

## Резюме выполненных исправлений

| Проблема | Статус | Коммит | Время |
|----------|--------|--------|-------|
| carbs → carbohydrates валидация | ✅ FIXED | d5a56b3, 9af0ec4 | 13:55 |
| LOG_LEVEL=DEBUG → INFO | ✅ FIXED | Конфигурация | 13:54 |
| chmod 600 .env | ✅ FIXED | Конфигурация | 13:54 |
| Docker bind 0.0.0.0 | ⏸️ ОТЛОЖЕНО | - | - |
| Модель gpt-5-image-mini | ⏸️ ОТЛОЖЕНО | - | - |

**Итоговый вердикт:** 🟢 **PRODUCTION READY**

---

## 1. Исправление валидации (BLOCKER)

### Проблема
```
HTTP/1.1 500 Internal Server Error
{
  "detail": "AI service error: Invalid data type in AI response:
  1 validation error for FoodItem\ncarbohydrates\n
  Field required [type=missing, input_value={...,'carbs': 0.0}]"
}
```

**Причина:**
- OpenRouter модель возвращала поле `carbs`
- Схема `FoodItem` ожидала `carbohydrates`
- Промпты также указывали `carbs` в примере JSON

### Решение

**Коммит 1: d5a56b3** - Нормализация полей перед валидацией

Добавлена функция `normalize_item_fields()`:
```python
def normalize_item_fields(item: dict) -> dict:
    """
    Normalize field names to match SSOT schema.
    LLM models may return alternative field names.
    """
    normalized = item.copy()

    # Normalize: carbs → carbohydrates
    if "carbs" in normalized and "carbohydrates" not in normalized:
        normalized["carbohydrates"] = normalized.pop("carbs")

    # Normalize: calories → kcal
    if "calories" in normalized and "kcal" not in normalized:
        normalized["kcal"] = normalized.pop("calories")

    return normalized
```

**Применение:**
```python
for item_data in data.get("items", []):
    normalized_item = normalize_item_fields(item_data)  # ← Нормализация
    items.append(FoodItem(**normalized_item))
```

**Также обновлены промпты:**
- Русский: `"carbs"` → `"carbohydrates"`
- Английский: `"carbs"` → `"carbohydrates"`

**Коммит 2: 9af0ec4** - Исправление расчёта totals

Обнаружена пропущенная ссылка на `.carbs` при расчёте итоговых значений:
```python
# Было:
carbs=sum(item.carbs for item in items)

# Стало:
carbohydrates=sum(item.carbohydrates for item in items)
```

### Результат после исправления

**Тестовый запрос:**
```bash
curl -X POST http://localhost:8001/api/v1/ai/recognize-food \
  -H "X-API-Key: c6b837b..." \
  -F "image=@test_food_image.jpg" \
  -F "locale=ru"
```

**Ответ:**
```json
{
  "items": [
    {
      "name": "turkey (roasted, skinless, assumed)",
      "grams": 150.0,
      "kcal": 202.5,
      "protein": 43.5,
      "fat": 2.25,
      "carbohydrates": 0.0  ← ✅ Правильное поле!
    },
    {
      "name": "potato (boiled, assumed)",
      "grams": 200.0,
      "kcal": 174.0,
      "protein": 4.0,
      "fat": 0.2,
      "carbohydrates": 40.2  ← ✅ Правильное поле!
    }
  ],
  "total": {
    "kcal": 376.5,
    "protein": 47.5,
    "fat": 2.45,
    "carbohydrates": 40.2  ← ✅ Правильное поле!
  },
  "model_notes": "..."
}
```

✅ **Статус:** HTTP 200 OK, валидация проходит, данные корректны.

---

## 2. LOG_LEVEL=DEBUG → INFO

### Проблема
```bash
LOG_LEVEL=DEBUG  # В продакшене
```

**Риски:**
- Избыточный объём логов
- Утечка чувствительных данных в DEBUG логах
- Потенциальные накладные расходы

### Решение
```bash
sed -i 's/LOG_LEVEL=DEBUG/LOG_LEVEL=INFO/' /opt/eatfit24-ai-proxy/.env
docker compose restart
```

### Результат
**Проверка логов:**
```json
{"ts": "2025-12-22T13:57:06", "level": "INFO", ...}
{"ts": "2025-12-22T13:57:06", "level": "INFO", ...}
```

✅ Все логи теперь на уровне INFO, DEBUG логов нет.

---

## 3. Права на .env файл

### Проблема
```bash
-rw-r--r-- 1 root root 307 Dec 21 13:57 .env  # Читаемо для группы и других
```

**Риск:** Секреты доступны всем пользователям системы.

### Решение
```bash
chmod 600 /opt/eatfit24-ai-proxy/.env
```

### Результат
```bash
-rw------- 1 root root 307 Dec 22 13:54 .env  # ✅ Только root
```

✅ Файл теперь доступен только владельцу (root).

---

## 4. Отложенные исправления

### 4.1 Docker bind 0.0.0.0:8001

**Текущее состояние:**
```yaml
ports:
  - "8001:8001"  # Bind на все интерфейсы
```

**Защита:**
```bash
ufw status
# 8001/tcp  ALLOW  100.0.0.0/8  # Только Tailscale
```

**Рекомендация (необязательная):**
```yaml
ports:
  - "100.84.210.65:8001:8001"  # Явная привязка к Tailscale IP
```

**Решение:** Отложить на будущее. Firewall обеспечивает достаточную защиту.

**Приоритет:** 🟡 SHOULD FIX (не блокер)

---

### 4.2 Нестандартная модель OpenRouter

**Текущая модель:**
```
OPENROUTER_MODEL=openai/gpt-5-image-mini
```

**Статус:**
- Модель не указана в официальной документации OpenRouter (декабрь 2025)
- Возможно, это бета-модель или устаревший ID

**Рекомендация:**
1. Проверить в OpenRouter Dashboard, что модель активна
2. Если модель работает → задокументировать в ENV_CONTRACT.md
3. Если модель deprecated → переключиться на `openai/gpt-4o-mini`

**Решение:** Отложить проверку. Пока модель работает корректно.

**Приоритет:** 🟡 SHOULD VERIFY (не блокер)

---

## 5. Текущее состояние сервера

### Контейнер
```
NAME                STATUS                    HEALTH
eatfit24-ai-proxy   Up 56 seconds             healthy
```

### Ресурсы
```
CONTAINER          CPU %     MEM USAGE / LIMIT   MEM %
eatfit24-ai-proxy  0.18%     41.83MiB / 512MiB   8.17%
```

✅ Контейнер здоров, ресурсы в норме.

### Последние коммиты
```
9af0ec4 fix: Use carbohydrates field in TotalNutrition calculation
d5a56b3 fix: Normalize LLM field names (carbs→carbohydrates) before schema validation
bf8cd08 Merge remote changes with repository cleanup
```

✅ Все фиксы развёрнуты на сервере.

### Логи (последний успешный запрос)
```json
{
  "ts": "2025-12-22T13:57:06",
  "level": "INFO",
  "logger": "app.openrouter_client",
  "msg": "OpenRouter token usage: prompt=1164, completion=1138, total=2302",
  "request_id": "7eac06a7"
}
{
  "ts": "2025-12-22T13:57:06",
  "level": "INFO",
  "logger": "app.main",
  "msg": "Successfully recognized 2 food items",
  "request_id": "7eac06a7"
}
{
  "ts": "2025-12-22T13:57:06",
  "level": "INFO",
  "logger": "app.main",
  "msg": "Request completed: POST /api/v1/ai/recognize-food -> 200",
  "request_id": "7eac06a7",
  "status": 200,
  "duration_ms": 19202.01
}
```

✅ API работает, токены логируются, ответы корректны.

---

## 6. Финальный чеклист

### Критические (MUST FIX)
- [x] Исправить валидацию `carbs` → `carbohydrates`
- [x] Изменить LOG_LEVEL=DEBUG → INFO
- [x] Установить chmod 600 на .env

### Желательные (SHOULD FIX)
- [ ] Привязать Docker к Tailscale IP (опционально, firewall достаточен)
- [ ] Проверить модель `openai/gpt-5-image-mini` (работает, но не задокументирована)

---

## 7. Итоговый вердикт

### 🟢 **PRODUCTION READY**

Сервис полностью готов к продакшену:

| Критерий | Статус |
|----------|--------|
| ✅ Код соответствует репозиторию | PASS |
| ✅ API работает без ошибок | PASS |
| ✅ Валидация данных корректна | PASS |
| ✅ Логи на уровне INFO | PASS |
| ✅ Секреты защищены (chmod 600) | PASS |
| ✅ Контейнер healthy | PASS |
| ✅ Ресурсы в норме | PASS |
| ✅ Firewall активен | PASS |
| ✅ Non-root user | PASS |

**Все критические блокеры устранены.**

---

## 8. Что было сделано (summary)

### Изменения в коде
1. **app/openrouter_client.py** - Добавлена нормализация полей LLM
   - Функция `normalize_item_fields()` для `carbs` → `carbohydrates`
   - Обновлены промпты (RU/EN) на `carbohydrates`
   - Исправлен расчёт `TotalNutrition`

### Изменения на сервере
2. **.env** - LOG_LEVEL=DEBUG → INFO
3. **.env** - chmod 644 → 600 (защита секретов)
4. **Сервис** - Перезапущен с новым кодом

### Проверки
5. **API тест** - Запрос с изображением → HTTP 200, корректный JSON
6. **Логи** - Подтверждено отсутствие DEBUG логов
7. **Контейнер** - Статус healthy, ресурсы в норме

---

## 9. Рекомендации на будущее

### Краткосрочные (1-2 недели)
1. Проверить модель `openai/gpt-5-image-mini` в OpenRouter Dashboard
2. Если модель deprecated → переключиться на `openai/gpt-4o-mini`
3. Задокументировать выбор модели в ENV_CONTRACT.md

### Среднесрочные (1 месяц)
4. Добавить интеграционные тесты с реальными изображениями
5. Настроить мониторинг (Prometheus/Grafana)
6. Добавить алертинг на 5xx ошибки

### Долгосрочные (3 месяца)
7. Рассмотреть привязку Docker к Tailscale IP (defense-in-depth)
8. Добавить CI/CD pipeline для автоматических деплоев
9. Реализовать rate limiting на уровне proxy

---

**Аудит и исправления завершены:** 2025-12-22 13:57 UTC
**Версия отчёта:** 1.0
**Статус:** 🟢 PRODUCTION READY
